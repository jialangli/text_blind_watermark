# 文本 / 图片隐水印工具 依赖
# 安装：pip install -r requirements.txt
# （tkinter 为 Python 标准库自带，无需单独安装；Windows 一般默认带，Linux 需 sudo apt install python3-tk）

numpy==1.26.0
Pillow==12.2.0
opencv-python==4.8.0.76
python-docx==1.2.0
PyMuPDF==1.28.2
