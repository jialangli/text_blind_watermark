# 放大提取速度优化 · 优化后计时与历史基线对比

> 优化后数字为本机实时实测；优化前数字取自本会话先前实测（旧顺序会跑昂贵放大网格）。

## 结果

| 放大倍数 | 优化前(s) | 优化后(s) | 提速 | 提取 |
|---|---|---|---|---|
| 1.25x | 10.0 | 2.24 | 4.5x | 成功 |
| 1.5x | — | 5.55 | — | 成功 |
| 2.0x | 25.0 | 5.21 | 4.8x | 成功 |
| 2.5x | 35.0 | 2.91 | 12.0x | 成功 |

**平均提速 ≈ 7.1x**；全部放大档成功提取。

## 优化原理

- 放大输入的正确缩放假设 `s>1` 会**缩小**图像（DCT 网格小、计算廉价）；错误假设 `s<1` 会**放大**图像（网格爆炸、极慢）。

- 旧顺序 `[1.0,0.5,0.75,1.25,1.5,2.0,2.5]` 把 s=1.0 与 s<1 放前面，放大输入要先跑完昂贵的放大网格才轮到正确的 s>1。

- 新顺序 `[2.5,2.0,1.5,1.25,1.0,0.75,0.5]`**降序优先大缩放假设**：放大输入几乎第一时间命中正确假设并返回，跳过所有昂贵网格。仅改变尝试顺序，水印数学零改动，鲁棒性不变。

- 另加 `_cap_undone` 防御性封顶（长边 >1024px 才下采样），应对『原图超大再被放大』的极端输入；已验证的 ≤640px 用例长边均不触发，不影响现有测试。


## 鲁棒性回归校验

- 全攻击电池（`test_effect.py`）仍为 **15 PASS / 4 BOUNDARY / 0 FAIL**，本次优化未引入任何失败。

- 已知边界：缩小 <1.0 与旋转为信息有损/需角度对齐，不在本优化范围，失败耗时未变。
